"use strict"
function sum(a,b){
  var add = a+b;
 
  //if I am not writing the 'var' then it will give the error!! when I am writing this "use strict".
  
  console.log(add);

  //it will give the output 30
}
sum(10,20);